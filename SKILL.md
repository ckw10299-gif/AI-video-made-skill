---
name: ai-video-made-skill
description: 生成可直接用于即梦、可灵、Seedance、Sora、Runway 等工具的中文 AI 视频提示词。支持根据角色图与剧情直接创作；根据本地视频或1至3条抖音链接自动下载、抽帧分析，再复刻结构、节奏、台词功能和镜头机制并完成游戏本地化。适用于剧情、展示、进化、黑化、技能、买量前贴、短剧冲突与批量视频本地化。
---

# AI-video-made-skill

生成结构清晰、可复制、可执行的中文 AI 视频提示词。严格区分“文生视频”和“视频生视频”，不要凭标题猜测视频内容，不要把上一任务的元素带入新任务。

## 能做什么

1. 文生视频：根据剧情、角色和素材直接创作剧情类或展示类提示词。
2. 视频生视频：读取本地视频、抽帧并分析构图、站位、动作、台词和节奏，再完成本地化。
3. 抖音复刻：处理1至3条抖音链接，自动下载、抽帧、逐条分析并输出同等数量的提示词。
4. 对已有提示词进行局部修改、格式转换、节奏压缩、分段生成和失败诊断。

需要判断画风、真人与动画成稿差异、镜头规则或提示词避坑时，读取 [references/production-guide.md](references/production-guide.md)。需要具体写法时，读取 [references/examples.md](references/examples.md)。任务提到《闪耀吧噜咪》、噜咪、彩色噜咪、星彩噜咪、自动捉宠或该项目本地化时，必须先读取 [references/shining-lumi-brief.md](references/shining-lumi-brief.md)，只使用其中已经确认的玩法、术语和卖点，不自行发明游戏系统。

## 首次调用

用户尚未按模板提供信息时，先弹出以下模板；用户信息已经充分时直接执行，不要求重复填写。

弹出模板前先用一句话介绍：本 Skill 支持文生视频、视频生视频和抖音链接复刻，可生成并持续修改完整中文视频提示词。

```text
【AI 视频需求填写模板】

（类型）：文生视频 / 视频生视频

（画风）：
例如：古早日本动漫、手绘赛璐璐、游戏三渲二、高级CG游戏PV

（时长）：
例如：5秒、10秒、15秒；单条或需要拆成多段

（出现角色）：
角色总数：
角色1：名称、剧情作用
角色2：名称、剧情作用

（大致剧情）：
文生视频填写开头、发展、转折、结尾；视频生视频填写需要替换或改编的内容。

（备注）：
产品名、核心卖点、固定台词、画幅、平台、必须保留或禁止出现的内容。
```

用户也可以跳过模板，直接发送1至3条抖音链接，并补充游戏名、核心卖点、角色替换和固定台词。此时自动进入“抖音链接批处理”工作流。

缺少非关键细节时做保守判断并继续。只有缺失信息会实质改变剧情、角色数量或产品卖点时才追问。

## 路由任务

### 文生视频

根据角色图、画风、时长和大致剧情直接设计。先判断为“展示类”还是“剧情类”。

### 视频生视频

必须先读取原视频并抽帧，再分析和本地化。未看到视频内容前不得声称已经复刻，也不得根据文件名自行编写分镜。

用户选择“视频生视频”时，输入必须是可读取的视频文件或有效视频链接；只有音频、截图或文件名时不开始复刻。先解决读取问题，再写提示词。

### 抖音链接批处理

用户一次提供1至3条抖音链接时，不要求用户先下载视频。按以下顺序自动完成：

1. 从用户消息中提取链接，按出现顺序编号，保留分享文案作为辅助信息。
2. 使用 `scripts/process-douyin-links.ps1` 下载、标准化、校验并抽帧；默认每条链接单独建目录。
3. 若脚本下载失败，使用可用的联网或浏览器工具打开链接，解析最终作品页和实际视频媒体地址，再下载到对应目录并重新运行 `scripts/analyze-video.ps1`。
4. 分别查看每条视频的 `metadata.json`、`opening.jpg`、`overview.jpg` 和 `scene-frames`；关键动作或台词不清楚时补充密集抽帧。
5. 对每条视频独立完成结构、场景、台词、镜头和卖点本地化，禁止把不同链接的剧情或角色混在一起。
6. 按链接顺序输出同等数量的完整中文提示词。每条提示词前先给出3至5条简短分析结论。

详细执行方式、目录结构、失败恢复和输出要求见 [references/douyin-link-workflow.md](references/douyin-link-workflow.md)。

批处理时采用故障隔离：某条链接下载或解析失败，不中止其他链接；先完成可处理项，再明确报告失败项及已自动尝试的恢复步骤。

## 分析视频

收到本地视频后执行 `scripts/analyze-video.ps1`：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/analyze-video.ps1 `
  -VideoPath "D:\path\video.mp4" `
  -OutputDirectory "D:\path\analysis"
```

查看脚本生成的 `metadata.json`、`overview.jpg`、`opening.jpg` 和 `scene-frames`。镜头或台词仍不清楚时，在关键时间段补充每0.5秒或1秒抽帧。

分析以下内容后再写提示词：

1. 类型：剧情类或展示类。
2. 前3秒钩子：危机、反差、荒诞行为、强视觉或紧急选择。
3. 结构：开场画面、冲突或展示触发、升级、反转、产品落点。
4. 视觉锚点：动作、道具、角色状态、场景、变身或UI机制。
5. 镜头语言：固定机位、推近、横移、跟拍、俯拍、反打、定格。
6. 台词功能：谁攻击、谁解释、谁反转；哪些原句保留或改写。
7. 场景本地化：原场景是否保留；改成游戏主城、森林、海滩、竞技场等后如何维持原构图和剧情功能。
8. 产品桥接：用真实游戏卖点替换原片表层元素，不复制竞品角色、名称或专属视觉。

在最终提示词前，用3至5条简短结论说明原片结构、场景本地化、台词调整和产品卖点迁移。

### 视频读取失败

不要找理由或直接让用户手动截图。依次处理：

1. 检查文件是否存在、路径是否正确、是否有读取权限。
2. 使用 `ffprobe` 检查编码、时长、画幅和音轨。
3. 使用 `ffmpeg` 转为 H.264 + AAC 的临时 MP4 后重试。
4. 降低抽帧分辨率或分段抽取。
5. 自动方法均失败后，明确说明失败环节和下一种可行方案，再请求用户补充素材。

不得在读取失败时虚构原视频内容。

## 设计展示类

用于进化、黑化、净化、变身、觉醒、时装、技能和角色展示。

遵循：

```text
原始形态建立 -> 局部变化 -> 变化扩散 -> 场景同步变化 -> 最终英雄画面
```

- 进化、黑化或净化必须在【场景】写出变化前、触发时和变化后的环境对比。
- 说明变化从哪里开始、如何沿身体扩散、材质和光影如何改变。
- 参考图中的起始形态和最终形态都要风格化到同一画风。
- 结尾保留清晰完整的最终形态，通常定格0.5至1秒。
- 不强行加入完整剧情或大量台词。

## 设计剧情类

用于买量前贴、生存钩子、对话冲突、爽文反转、悬念短剧和荒诞喜剧。

- 前3秒必须出现明确钩子。
- 角色立场和冲突必须能从动作中看懂。
- 每个镜头只承担一个主要动作或信息。
- 台词要短、口语化、有明确情绪；用户提供的固定台词不得擅改。
- 用户要求“完全复刻”时，必须逐项复刻画面构图、机位、景别、运镜状态、角色站位、动作节奏和台词，只替换用户明确指定的角色、道具、场景或游戏元素。原片是固定镜头时保持固定镜头，不得自行增加推近、拉远、环绕、摇镜或切镜。
- 用户明确给出台词时，台词必须逐字保留，不替换措辞、不补语气词、不增加新台词。只允许修改用户点名需要本地化的专有名词。
- 用户只要求保留原片某一片段时，只复刻该片段的时长、动作、构图和台词，不续写后续剧情，不为了凑时长增加分镜。
- 反转要由可见动作完成，不能只靠旁白解释。
- 总时长必须等于各分镜时间之和，台词长度必须能在对应时段说完。

每个分镜必须包含：

```text
分镜N
时间轴：
运镜：
景别：
场景：
画面：
具体内容：
配音：角色，声线：“台词”
```

无台词时将最后一项写为“音效”。不要使用“字幕感文案”这个标签。

## 写角色

【角色】只负责绑定角色图、基础身份、剧情作用和声线，保持简短：

```text
光头：参考角色图1，圆胖的光头男性，担任主角；憨厚亢奋的男性声线。
```

- 不复述立绘的全部服装和身体细节。
- 不自行增加可能与角色图冲突的发型、配饰、花纹或体型。
- 用户提供角色素材时，只在【角色】中绑定；素材顺序不稳定时写“形象以对应角色素材为准”，不要写图片编号。
- 用户限制参考图数量时，减少需要精确控制的角色；把视觉重心集中到一个核心角色或宠物。
- 多形态变化时明确哪些特征必须继承，防止融合、重复和串色。
- 输入阶段只要求“角色名称+剧情作用”；详细形象、性格、服装和声线由生成阶段结合剧情与素材补全。
- 涉及“少年、萝莉、小孩”等容易触发年龄误判的称呼时，统一改用“男、女、角色”，不主动强调年龄。

## 写其他参考

只有关键物品确实需要用户提供素材时才增加【其他参考】，列出宠物蛋、噜咪、进化果实、武器、服装或场景等对象。使用对象名称绑定素材，不使用图片编号，不添加用户不会提供的参考物。

## 写镜头

把运镜与场景切换写清楚，但不要过度复杂。优先使用：

- 固定机位
- 快速推近或拉远
- 侧向平行跟拍
- 简单俯拍或仰拍
- 正反打
- 甩镜切换
- 动作匹配转场
- 短暂动作定格

避免在同一个镜头中叠加环绕、旋转、变焦、俯冲和多次转场。固定视角的原片必须明确写“全程固定机位、固定视角、角色或场景自身移动”。

描述画面视角与跟随关系，不描述摄影器材本身进入现场。例如写“正面跟拍，角色持续向前，主体大小稳定”，不要写“摄影机安装在稳定器上后退”。方向关系必须符合空间逻辑，例如车内视角中的人物应平行于车窗奔跑，而不是朝车窗冲来。

## 区分动画与真人成稿

### 动画、三渲二、赛璐璐

剧情类可使用结构化分镜，每个分镜写清时间轴、运镜、景别、场景、画面、具体内容和配音。展示类可按连续变化阶段写时间轴，不强行加入大量分镜或台词。

### 真人短剧

仍按分镜与时间轴组织，但每个分镜使用白描式连续段落，把视角、人物位置、动作、表情、他人反应和台词按发生顺序写在一起。不要把运镜、景别、配音拆成互相分离的字段，以免模型打乱先后关系。每段只安排一个核心动作，其他人物必须有可见反应。

详细差异见 [references/production-guide.md](references/production-guide.md)。

## 控制连续变化

角色边走边变身、飞行中换装或宠物奔跑中进化时：

- 保持动作方向、步态或飞行姿势连续。
- 让变化贴着身体或从明确部位开始。
- 不停下蓄力，不突然摆展示姿势，不凭空替换角色。
- 最终形态保持相同五官、体型与身份，除非用户明确要求改变。

## 分段生成

用户需要把25秒左右视频拆成两段时，将每段控制在约12至15秒，并保证：

- 两段提示词各自重新写完整的【画面风格】【场景】【角色】。
- 第二段不能假设模型知道第一段内容、角色状态或最后一帧。
- 第二段开头明确重建承接画面和角色位置。
- 删除重复剧情，避免第二段重演第一段结尾。
- 两段使用一致的画幅、画风、光线、角色称呼和动作方向。

## 输出结构

最终提示词固定按以下顺序输出：

1. 【画面风格】
2. 【场景】
3. 【角色】
4. 【其他参考】（存在关键物品素材时）
5. 【具体内容】
6. 【正向提示词】
7. 【镜头设计思路】

用户只要求修改其中一部分时，只输出该部分；用户需要可直接复制的完整版本时输出全部七部分；没有关键物品素材时省略【其他参考】。

### 正向提示词

【正向提示词】只写生成方向，不写剧情摘要：

- 画幅、媒介、年代、渲染风格
- 光影、色彩、材质、镜头语言
- 节奏、动作质量、角色稳定性
- 需要避免的视觉问题

不要把具体人物、怪物、地点、台词和完整动作链压缩进【正向提示词】。

## 最终成稿纯净化

- 最终提示词必须脱离当前对话独立成立，不出现“原视频、参考视频、复刻原片、上一段、根据前文、用户提供”等工作过程词。
- 用户说某元素不需要时，直接从成稿中删除，不写“不要出现、禁止出现、不能出现、不切换到”等反向提醒。改用需要的场景、主体和动作正向限定。
- 用户给出固定台词时逐字保留，只修改用户点名需要本地化的专有名词；不补语气词、不润色、不增加新台词。
- 不在台词里擅自加入角色姓名。角色名只用于脚本标识，除非台词原本就称呼该名字。
- 设备有明确现实名称时直接写“手机、平板、电视”，不用“发光设备”等含糊词。

## 质量检查

输出前逐项检查：

- 是否真正读取并分析了原视频。
- 是否复刻了结构、节奏和镜头机制，而非只换角色名称。
- 是否完成场景和台词本地化。
- 是否误带入上一任务的数字门、丧尸、海啸等无关元素。
- 所有固定台词是否原样保留。
- 角色描述是否过长或与角色图冲突。
- 每个剧情分镜是否包含全部七项。
- 运镜是否清楚且不过度复杂。
- 时间轴之和是否符合目标时长。
- 【正向提示词】是否仍是生成方向而非剧情摘要。
- 多段生成是否彼此独立且可以直接拼接。
- 是否出现用户禁止的手机、UI、字幕、额外角色或特效。
- 最终成稿是否仍包含“不要、禁止、不能、原视频、参考视频”等过程性或反向措辞。
- 真人短剧是否使用按时间发生的白描式段落，动画分镜是否包含所需字段。
- 所有出场人物是否对关键结果作出表情或动作反馈。

## 示例

需要参考写法时读取 [references/examples.md](references/examples.md)。实例覆盖文生剧情、展示进化、三渲二技能、动画固定镜头复刻、真人短剧复刻、抖音链接本地化、像素Boss和长视频拆段。
